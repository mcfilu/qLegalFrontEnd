import React from 'react'
import { spotify, } from '../assets'
import { buzzsprout } from '../assets'
import { apple } from '../assets'
import { spotifyPodcasts } from '../assets'
import { podcastLinks } from '../constants'

export default function LinksModal ({visible}){

    if (!visible){
        return null
    }
    else {
        return (
            
          )
    }
    
}